<?xml version="1.0" encoding="utf-8"?>
<!-- Only mod-specific strings live here; the tuning gizmo, inspect string and settings window
     reuse vanilla keys where the text is identical. NoFoggedDeposits is a lower-case fragment:
     it is CapitalizeFirst()ed where shown (forced-job fail text, inspect line). -->
<LanguageData>
  <LocalMineralScanner_LetterLabelFoundDeposit>Mineral deposit located</LocalMineralScanner_LetterLabelFoundDeposit>
  <LocalMineralScanner_LetterFoundDeposit>{FINDER_nameDef} has located a deposit of {0} in an undiscovered area using the local mineral scanner.</LocalMineralScanner_LetterFoundDeposit>
  <LocalMineralScanner_MessageExhausted>No more undiscovered {0} deposits remain on this map. The local mineral scanner will remain idle until it is tuned to another mineral.</LocalMineralScanner_MessageExhausted>
  <LocalMineralScanner_NoFoggedDeposits>no undiscovered {0} deposits left</LocalMineralScanner_NoFoggedDeposits>
  <LocalMineralScanner_NoneUndiscovered>none undiscovered</LocalMineralScanner_NoneUndiscovered>
  <LocalMineralScanner_CombinedScanSpeed>Combined scanning speed ({0} operators)</LocalMineralScanner_CombinedScanSpeed>

  <!-- Settings window. *Desc keys are hover tooltips. SettingsCategory is also the Workshop
       title and must equal the first line of .steamworkshop/Description/<Language>.txt. -->
  <LocalMineralScanner_SettingsCategory>Local Mineral Scanner</LocalMineralScanner_SettingsCategory>
  <LocalMineralScanner_SettingsPlacement>Placement</LocalMineralScanner_SettingsPlacement>
  <LocalMineralScanner_SettingsScanning>Scanning</LocalMineralScanner_SettingsScanning>
  <!-- Appended to a slider label; keep the leading space. -->
  <LocalMineralScanner_DefaultSuffix> (default)</LocalMineralScanner_DefaultSuffix>

  <!-- {0} = vanilla building label (long-range mineral scanner / deep drill). -->
  <LocalMineralScanner_RequireUnroofed>Doesn't work under a roof</LocalMineralScanner_RequireUnroofed>
  <LocalMineralScanner_RequireUnroofedDesc>Like the {0}, it can't be placed or operated under a roof. Turn off to allow.</LocalMineralScanner_RequireUnroofedDesc>
  <LocalMineralScanner_Minifiable>Can be uninstalled</LocalMineralScanner_Minifiable>
  <LocalMineralScanner_MinifiableDesc>Lets the scanner be uninstalled and moved, like the {0}. Turn off and it can only be deconstructed.</LocalMineralScanner_MinifiableDesc>
  <LocalMineralScanner_MassDesc>Weight of the uninstalled scanner when hauled or carried by a caravan.</LocalMineralScanner_MassDesc>

  <!-- {0} = the long-range mineral scanner's label, {1} = its own value for this setting. -->
  <LocalMineralScanner_FindMtbDaysDesc>Average working days between finds for one operator at 100% research speed. The {0} uses {1}.</LocalMineralScanner_FindMtbDaysDesc>
  <LocalMineralScanner_FindGuaranteedDays>Guaranteed find within</LocalMineralScanner_FindGuaranteedDays>
  <LocalMineralScanner_FindGuaranteedDaysDesc>Working days after which a find is guaranteed, for one operator at 100% research speed. The {0} uses {1}.</LocalMineralScanner_FindGuaranteedDaysDesc>

  <!-- {0} = resource label (lower case, row is CapitalizeFirst()ed), {1} = count. -->
  <LocalMineralScanner_ResourceCost>{0} cost: {1}</LocalMineralScanner_ResourceCost>
  <LocalMineralScanner_ResourceCostDesc>Amount of {0} needed to build one scanner. 0 removes it from the cost.</LocalMineralScanner_ResourceCostDesc>
</LanguageData>
