<?xml version="1.0" encoding="utf-8"?>
<Patch>

  <!-- Architect > Misc ordering: slot the local scanner directly after vanilla's
       long-range mineral scanner.

       How the grid orders build gizmos (decompile-verified, 1.6):
       - Designator_Build.Order returns BuildableDef.uiOrder (float, default 2999f).
       - GizmoGridDrawer.DrawGizmoGrid stable-sorts the tab's designators by Order
         (GenCollection.SortStable); DesignationCategoryDef and ArchitectCategoryTab do
         no ordering of their own.
       - Ties keep DefDatabase order: Core, then DLCs, then mods. A modded def tied on
         uiOrder therefore always lands after every vanilla def sharing the value.

       Core leaves six Misc buildings on the 2999 default, in this XML order: MoisturePump,
       GroundPenetratingScanner, LongRangeMineralScanner, MultiAnalyzer, VitalsMonitor,
       ToolCabinet. With no uiOrder of our own we'd tie at 2999 and sort after all six.
       There is no value strictly between two tied defs, so the only way in is to lower
       the long-range scanner below 2999 and place ours just above it.

       The values below (2990/2991/2992, ours 2993 in Buildings_LocalMineralScanner.xml)
       are position-preserving for vanilla: those three are already first in the 2999
       group and stay first, in the same relative order, still behind Odyssey's 2500/2600
       entries. The moisture pump is included only for that reason: leaving it at 2999
       would move it from ahead of the scanners to behind ours.

       Each def is patched via PatchOperationConditional so the operation replaces an
       existing uiOrder rather than adding a duplicate node, which the XML loader rejects
       ("defines the same field twice"). -->

  <Operation Class="PatchOperationConditional">
    <xpath>Defs/ThingDef[defName="MoisturePump"]/uiOrder</xpath>
    <match Class="PatchOperationReplace">
      <xpath>Defs/ThingDef[defName="MoisturePump"]/uiOrder</xpath>
      <value><uiOrder>2990</uiOrder></value>
    </match>
    <nomatch Class="PatchOperationAdd">
      <xpath>Defs/ThingDef[defName="MoisturePump"]</xpath>
      <value><uiOrder>2990</uiOrder></value>
    </nomatch>
  </Operation>

  <Operation Class="PatchOperationConditional">
    <xpath>Defs/ThingDef[defName="GroundPenetratingScanner"]/uiOrder</xpath>
    <match Class="PatchOperationReplace">
      <xpath>Defs/ThingDef[defName="GroundPenetratingScanner"]/uiOrder</xpath>
      <value><uiOrder>2991</uiOrder></value>
    </match>
    <nomatch Class="PatchOperationAdd">
      <xpath>Defs/ThingDef[defName="GroundPenetratingScanner"]</xpath>
      <value><uiOrder>2991</uiOrder></value>
    </nomatch>
  </Operation>

  <Operation Class="PatchOperationConditional">
    <xpath>Defs/ThingDef[defName="LongRangeMineralScanner"]/uiOrder</xpath>
    <match Class="PatchOperationReplace">
      <xpath>Defs/ThingDef[defName="LongRangeMineralScanner"]/uiOrder</xpath>
      <value><uiOrder>2992</uiOrder></value>
    </match>
    <nomatch Class="PatchOperationAdd">
      <xpath>Defs/ThingDef[defName="LongRangeMineralScanner"]</xpath>
      <value><uiOrder>2992</uiOrder></value>
    </nomatch>
  </Operation>

</Patch>
